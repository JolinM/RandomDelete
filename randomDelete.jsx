//randomDelete 1.0//created by Jolin Masson//www.jolinmasson.comvar select = app.activeDocument.selection;var count = select.length;if ( count === 0 ) {	alert ("You need to select at least one item")} else {	var promptPercentage = Number(prompt ("Which percentage to delete?", 50, "Delete Random Items"));	var percentage = count * promptPercentage / 100;	shuffle(select);	for ( var j = 0 ; j < percentage ; j++ ) {		select[j].remove ();	}}; //suffle Fisher-Yates, http://bost.ocks.org/mike/shuffle/function shuffle(array) {	var m = array.length, t, i;	// While there remain elements to shuffle…	while (m) {		// Pick a remaining element…		i = Math.floor(Math.random() * m--);		// And swap it with the current element.		t = array[m];		array[m] = array[i];		array[i] = t;	}	return array;}